apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crtsh-kubernetes.fullname" . }}-test-connection"
  labels:
    {{- include "crtsh-kubernetes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crtsh-kubernetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
